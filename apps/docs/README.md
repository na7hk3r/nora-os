# Nora Docs App

Reserved workspace for the future public/internal documentation app.

The current technical documentation remains in the repository-level `docs/`
directory while the product is migrated incrementally.

When this app becomes active, it should consume the existing Markdown docs
instead of duplicating them. CI currently treats this as a reserved workspace;
the production documentation source of truth remains `docs/`.
