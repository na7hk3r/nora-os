# Nora Assets

Canonical shared asset package for Nora OS.

- `brand-kit/`: original logo/isotype PNGs and the visual identity PDF.
- App-specific optimized copies can still live in each app while migration is in progress.

Treat this package as the canonical asset source. Generated or resized outputs
should either be committed in the app that needs them or ignored as build
artifacts.
