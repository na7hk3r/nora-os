# Nora Design System

Compatibility placeholder.

The shared design-system source of truth now lives in `packages/ui` as the
`@nora/ui` workspace package.

Do not add new tokens here. Keep this directory only as a compatibility pointer
until all references have moved to `packages/ui`.
