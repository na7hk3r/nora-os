# Nora Utils

Small reusable helpers shared across Nora packages.

Use this package for deterministic, dependency-light helpers such as dates,
strings, guards, parsers, and validation primitives.

Keep helpers framework-free and covered by package-level tests once they have
more than one consumer.
